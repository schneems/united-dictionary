If you can see this please contact the administrator, with instructions on how you got here plz.
<!--
<p class = "comment_label"><%= t 'comments.add_comment'%></p>
	<% form_for @comment,:url => {:id => "#{@phrase.id}", :controller => 'comments', :action => 'create'} do |f| %>
		<%= f.label :name, '#{t 'comments.name'}' %>:
		<%= f.text_field(:name, :value => params[:name_request])%><br />
		<%= f.label :comment_field, 'Your Comments' %>:<br />
		<%= f.text_area(:comment_field, :value => params[:comment_field_request])%><br />			
		<% if logged_in? %>	
				<div id = "new_captcha_position">
				   <img src="/images/dino.gif" height = "90px" width = "90x" /> 
				   <p>Thanks for Signing In!
				</div>
			<% else %>	
			<div id="comment_replace_captcha">					 
					<div id ="new_captcha_position">
						<%= show_simple_captcha %>
						<sub><center>Login to Skip Captcha</center></sub>
					</div> 
					<br /><br />				
		<% end %>
			<%= f.submit "Submit your Comment",:id => "comment_submit"%>	
<% end %> -->